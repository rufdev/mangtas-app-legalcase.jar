import com.rameses.annotations.*;

class HousingPersistence2Interceptor {
	
	@Env
	def env

	@Before(pattern="HousingLedgerBillingService.*", exclude="(SessionService|interceptors/WebSessionInterceptor).*",eval="#{env.CLIENTTYPE == 'json' && !env.WEB_SESSION_PASSED}",index=-1000) 
	void checkeBfore( def evt ) {
		env.WEB_SESSION_PASSED = true
		throw new Exception("TEST")
	}	


}